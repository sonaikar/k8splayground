# To create docker registry login secret

kubectl create secret docker-registry secret-tiger-docker \
  --docker-username=tiger \
  --docker-password=pass113 \
  --docker-email=tiger@acme.com



kubectl create secret docker-registry secret-artifactory-creds \
  --docker-username=artifactoryuser \
  --docker-password=artifactorypassword \
  --docker-email=sameer@cetbiz.com


TLS Secret:   https (ssl)

nginx webservers:
   ssl certifcate
   ssl key